export class MailStructure {
    constructor(status, name, email, message, recipient) {
        this.status = status;
        this.name = name;
        this.email = email;
        this.message = message;
        this.recipient = recipient
    }
}